#include "options.h"

namespace evworker::options {
  const char *NUM_WORKERS {"num-workers"};
  const char *PID_DIRECTORY {"pid-dir"};
  const char *CONFIG_PATH_SWITCH = "-C";
  const char *CONFIG_PATH = "config-path";
  const char *WORKER_EXE_PATH = "worker-exe-path";
}